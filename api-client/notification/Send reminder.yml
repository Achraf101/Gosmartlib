info:
  name: Send reminder
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/notify/1
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

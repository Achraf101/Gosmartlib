info:
  name: change role
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/session/LEERKRACHT
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

package be.ap.backend.enums;

/**
 * Represents the lifecycle status of a loan request and its processing state.
 */
public enum LoanStatus {
    REQUESTED,
    RECEIVED,
    RETURNED,
    ACCEPTED,
    DECLINED,
}
